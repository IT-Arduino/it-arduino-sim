# One group, no blank lines inside it: some crawlers treat a blank line as
# the end of a group, which left the Disallow rules below orphaned.
User-agent: *
Allow: /
# UUID-based duplicate of /:username/:slug (the canonical URL); crawling it
# is wasted budget.
Disallow: /project/
# API endpoints are not pages.
Disallow: /api/
# Auth and admin pages are deliberately NOT disallowed here: they carry a
# noindex meta tag, and a page has to be crawlable for that tag to be seen —
# a disallowed URL can still be indexed from links, title-less.

# Карта сайта — маршруты, известные на момент сборки (генерирует
# scripts/generate-sitemap.ts из frontend/src/seoRoutes.ts).
#
# У апстрима здесь было ещё две карты: sitemap-projects.xml для публичных
# проектов пользователей и docs/sitemap-index.xml от отдельной сборки портала
# документации. Ни того, ни другого в форке нет, а ссылка на несуществующую
# карту — это ошибка 404 в отчёте вебмастера при каждом обходе.
Sitemap: https://sim.it-arduino.ru/sitemap.xml
